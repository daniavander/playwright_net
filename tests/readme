1. install playwright

2. dd playwright mcp - https://github.com/microsoft/playwright-mcp
 install: ctrl shift p > install server
 in setting.json have to see:
 "servers": {
    >>running<< , stop, restart
      "playwright": {
        "command": "npx",
        "args": [
          "@playwright/mcp@latest"
        ]
      }
open copilot chat set to agent mode